From 713050d940b5bf9eb6fe7e30990facff4faffd77 Mon Sep 17 00:00:00 2001
From: Ryan McCabe <rmccabe@redhat.com>
Date: Mon, 11 Jun 2012 09:20:18 -0400
Subject: [PATCH] Remove trailing commas that cause problems for some old
 versions of MSIE

Resolves: rhbz#881796

Signed-off-by: Ryan McCabe <rmccabe@redhat.com>
---
 luci/public/js/add_nodes.js     | 2 +-
 luci/public/js/admin.js         | 2 +-
 luci/public/js/cluster_list.js  | 4 ++--
 luci/public/js/failover_form.js | 2 +-
 luci/public/js/fence.js         | 4 ++--
 luci/public/js/homebase.js      | 2 +-
 luci/public/js/resource.js      | 2 +-
 luci/public/js/service.js       | 2 +-
 8 files changed, 10 insertions(+), 10 deletions(-)

diff --git a/luci/public/js/add_nodes.js b/luci/public/js/add_nodes.js
index a132603..0ca79ea 100644
--- a/luci/public/js/add_nodes.js
+++ b/luci/public/js/add_nodes.js
@@ -9,6 +9,6 @@ $(function() {
         closeOnEscape: false,
         open: function(event, ui) { 
             $(this).parent().children().children('.ui-dialog-titlebar-close').hide();
-        }, 
+        }
     });
 })
diff --git a/luci/public/js/admin.js b/luci/public/js/admin.js
index b09672c..88ab090 100644
--- a/luci/public/js/admin.js
+++ b/luci/public/js/admin.js
@@ -9,6 +9,6 @@ $(function() {
         closeOnEscape: false,
         open: function(event, ui) {
             $(this).parent().children().children('.ui-dialog-titlebar-close').hide();
-        },
+        }
     });
 })
diff --git a/luci/public/js/cluster_list.js b/luci/public/js/cluster_list.js
index d10c136..83e591e 100644
--- a/luci/public/js/cluster_list.js
+++ b/luci/public/js/cluster_list.js
@@ -9,7 +9,7 @@ $(function() {
         closeOnEscape: false, 
         open: function(event, ui) { 
             $(this).parent().children().children('.ui-dialog-titlebar-close').hide();
-        },
+        }
     });
 })
 
@@ -24,6 +24,6 @@ $(function() {
         closeOnEscape: false,
         open: function(event, ui) { 
             $(this).parent().children().children('.ui-dialog-titlebar-close').hide();
-        }, 
+        }
     });
 })
diff --git a/luci/public/js/failover_form.js b/luci/public/js/failover_form.js
index 9bc865d..b0445b3 100644
--- a/luci/public/js/failover_form.js
+++ b/luci/public/js/failover_form.js
@@ -9,6 +9,6 @@ $(function() {
         closeOnEscape: false,
         open: function(event, ui) { 
             $(this).parent().children().children('.ui-dialog-titlebar-close').hide();
-        }, 
+        }
     });
 })
diff --git a/luci/public/js/fence.js b/luci/public/js/fence.js
index 78c23b3..b15b90e 100644
--- a/luci/public/js/fence.js
+++ b/luci/public/js/fence.js
@@ -9,7 +9,7 @@ $(function() {
         closeOnEscape: false,
         open: function(event, ui) { 
             $(this).parent().children().children('.ui-dialog-titlebar-close').hide();
-        }, 
+        }
     });
 
     $('#create_fencemethod_dialog').dialog({
@@ -22,7 +22,7 @@ $(function() {
         closeOnEscape: false,
         open: function(event, ui) { 
             $(this).parent().children().children('.ui-dialog-titlebar-close').hide();
-        }, 
+        }
     });
 })
 
diff --git a/luci/public/js/homebase.js b/luci/public/js/homebase.js
index 854b8bb..c8c59c0 100644
--- a/luci/public/js/homebase.js
+++ b/luci/public/js/homebase.js
@@ -9,6 +9,6 @@ $(function() {
         closeOnEscape: false,
         open: function(event, ui) { 
             $(this).parent().children().children('.ui-dialog-titlebar-close').hide();
-        }, 
+        }
     });
 })
diff --git a/luci/public/js/resource.js b/luci/public/js/resource.js
index f04a508..8e6f432 100644
--- a/luci/public/js/resource.js
+++ b/luci/public/js/resource.js
@@ -9,7 +9,7 @@ $(function() {
         closeOnEscape: false,
         open: function(event, ui) { 
             $(this).parent().children().children('.ui-dialog-titlebar-close').hide();
-        },
+        }
     });
 })
 
diff --git a/luci/public/js/service.js b/luci/public/js/service.js
index d409388..8331fdb 100644
--- a/luci/public/js/service.js
+++ b/luci/public/js/service.js
@@ -86,7 +86,7 @@ $(function() {
         closeOnEscape: false,
         open: function(event, ui) { 
             $(this).parent().children().children('.ui-dialog-titlebar-close').hide();
-        },
+        }
     });
 })
 
-- 
1.7.11.7

